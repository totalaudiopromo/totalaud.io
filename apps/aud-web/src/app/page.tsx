/**
 * Home Page - Landing Page
 * Phase 14: Shows standalone landing page instead of onboarding
 * Onboarding moved to /console route
 */

import { LandingPage } from '@/components/landing/LandingPage'

export default function HomePage() {
  return <LandingPage />
}
